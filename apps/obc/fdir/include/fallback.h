#ifndef OBC_FDIR_FALLBACK_H
#define OBC_FDIR_FALLBACK_H

#include "obc_supervisor_protocol.h"
#include "obc_ipc.h"

int fallback_request(supervisor_cmd_t cmd, OBC_Roles_t role);

#endif OBC_FDIR_FALLBACK_H
